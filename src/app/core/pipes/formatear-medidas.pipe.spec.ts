import { FormatearMedidasPipe } from './formatear-medidas.pipe';

describe('FormatearMedidasPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearMedidasPipe();
    expect(pipe).toBeTruthy();
  });
});
