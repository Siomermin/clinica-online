import { SeleccionarImagenDirective } from './seleccionar-imagen.directive';

describe('SeleccionarImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new SeleccionarImagenDirective();
    expect(directive).toBeTruthy();
  });
});
